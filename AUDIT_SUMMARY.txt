PRODUCT IMAGE AUDIT SUMMARY

1. EXISTING FUNCTIONALITY:
   - Product model has image_url field (VARCHAR(500), nullable)
   - Schemas support image_url in Create/Update/Read endpoints
   - Endpoints pass through client-provided image_url (no validation)
   - No file upload handling exists anywhere in backend
   - 8/8 product tests pass
   - Pillow 12.1.1 and python-multipart 0.0.27 installed but unused

2. PRODUCT MODEL (app/models/product.py:22):
   - image_url: Mapped[str | None] = mapped_column(String(500), nullable=True)
   - No file-storage system exists
   - No relationships to stored files
   - Field stores client-provided URL or path

3. CURRENT STORAGE:
   - NO upload endpoints exist
   - NO storage configuration
   - NO cloud storage configured
   - NO local upload directory defined
   - Pillow and python-multipart installed but unused

4. RECOMMENDED DESIGN:
   - Add local file upload with server-generated UUID filenames
   - Validate extensions (.jpg, .png, .webp, .gif)
   - Enforce 5MB file size limit
   - Admin-only upload/replacement permissions
   - Store in static/uploads/ directory
   - image_url stores path like /uploads/uuid.jpg

5. SUPPORTED IMAGE TYPES:
   - .jpg, .jpeg, .png, .webp, .gif

6. MAXIMUM FILE SIZE:
   - 5 MB (configurable)

7. REQUIRED DATABASE CHANGES:
   - NONE - existing image_url field sufficient

8. REQUIRED FILES:
   - app/core/config.py (upload dir + max size settings)
   - app/services/product_image.py (image service)
   - app/schemas/product_image.py (upload schemas)
   - app/main.py (upload endpoints + modify product update)
   - static/uploads/ directory

9. REQUIRED ENDPOINTS:
   - POST /products/{product_id}/upload-image (admin)
   - DELETE /products/{product_id}/image (admin, optional)
   - PUT /products/{product_id} (existing, image_url update)

10. SECURITY RULES:
    - Validate extension against allowed list (.jpg, .png, .webp, .gif)
    - Enforce file-size limit (5MB)
    - Generate server-side UUID filenames
    - Prevent path traversal
    - Admins only for image upload/replacement
    - Customers cannot upload/replace images

11. REQUIRED TESTS:
    - Admin can upload image to product
    - Non-admin gets 403 on upload
    - Invalid extension -> 400
    - File > 5MB -> 400/413
    - Non-image file (.exe) -> 400
    - Corrupt binary -> 400 (Pillow verify)
    - Image URL stored correctly after upload
    - Product image can be updated by admin
    - Product with no image still works
    - Image URL returned in ProductRead/ListRead
    - Image removal sets image_url to None

12. POTENTIAL RISKS:
    - Disk space if not managed on deletion
    - UUID filenames prevent collisions
    - Pillow may fail on unusual formats
    - Public serving of uploads needs care
    - Size limits prevent DoS

13. IMPLEMENTATION PRIORITY:
    1. app/core/config.py - settings
    2. app/services/product_image.py - service
    3. app/main.py - endpoints + validation
    4. app/schemas/product_image.py - schemas
    5. Tests
    6. Full test suite verification

14. FILES CHANGED FROM DELIVERY-FEE TASK:
    - app/models/delivery_config.py ✓
    - alembic/versions/11351_add_delivery_config_table.py ✓
    - app/schemas/delivery_fee.py ✓
    - app/schemas/common.py ✓
    - app/main.py (admin endpoints + checkout calc) ✓

15. FINAL TEST TOTALS (after delivery-fee):
    - 69 tests pass, 2 pre-existing address failures
    - Payments: 8/8 tests pass
    - Expenses: all 17 tests pass
    - Products: 8/8 tests pass